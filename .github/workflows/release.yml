name: Build Release APK & AAB

on:
  push:
    branches: ["**"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v3

      - name: Decode Firebase google-services.json
        env:
          GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: |
          if [ -z "$GOOGLE_SERVICES_JSON_BASE64" ]; then
            echo "GOOGLE_SERVICES_JSON_BASE64 secret is not set" >&2
            exit 1
          fi
          echo "$GOOGLE_SERVICES_JSON_BASE64" | base64 --decode > app/google-services.json

      - name: Decode signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "KEYSTORE_BASE64 secret is not set" >&2
            exit 1
          fi
          echo "$KEYSTORE_BASE64" | base64 --decode > app/release.keystore

      - name: Write keystore.properties
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          cat > keystore.properties <<EOF
          storeFile=../app/release.keystore
          storePassword=$KEYSTORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEY_PASSWORD
          EOF

      - name: Write gradle.properties secrets (AdMob)
        env:
          ADMOB_APP_ID: ${{ secrets.ADMOB_APP_ID }}
          ADMOB_BANNER_AD_UNIT_ID: ${{ secrets.ADMOB_BANNER_AD_UNIT_ID }}
          ADMOB_REWARDED_INTERSTITIAL_AD_UNIT_ID: ${{ secrets.ADMOB_REWARDED_INTERSTITIAL_AD_UNIT_ID }}
          ADMOB_INTERSTITIAL_AD_UNIT_ID: ${{ secrets.ADMOB_INTERSTITIAL_AD_UNIT_ID }}
        run: |
          {
            echo "ADMOB_APP_ID=$ADMOB_APP_ID"
            echo "ADMOB_BANNER_AD_UNIT_ID=$ADMOB_BANNER_AD_UNIT_ID"
            echo "ADMOB_REWARDED_INTERSTITIAL_AD_UNIT_ID=$ADMOB_REWARDED_INTERSTITIAL_AD_UNIT_ID"
            echo "ADMOB_INTERSTITIAL_AD_UNIT_ID=$ADMOB_INTERSTITIAL_AD_UNIT_ID"
          } >> gradle.properties

      - name: Write Play Games Services configuration
        env:
          PGS_APP_ID: ${{ vars.PGS_APP_ID }}
          PGS_ACH_FIRST_FEED: ${{ vars.PGS_ACH_FIRST_FEED }}
          PGS_ACH_SPARKLING_CLEAN: ${{ vars.PGS_ACH_SPARKLING_CLEAN }}
          PGS_ACH_FIRST_MINIGAME: ${{ vars.PGS_ACH_FIRST_MINIGAME }}
          PGS_ACH_HIGH_SCORE: ${{ vars.PGS_ACH_HIGH_SCORE }}
          PGS_ACH_LEVEL_5: ${{ vars.PGS_ACH_LEVEL_5 }}
          PGS_ACH_LEVEL_10: ${{ vars.PGS_ACH_LEVEL_10 }}
          PGS_ACH_STREAK_3: ${{ vars.PGS_ACH_STREAK_3 }}
          PGS_ACH_STREAK_7: ${{ vars.PGS_ACH_STREAK_7 }}
          PGS_ACH_DECORATOR: ${{ vars.PGS_ACH_DECORATOR }}
          PGS_ACH_COLLECTOR: ${{ vars.PGS_ACH_COLLECTOR }}
          PGS_ACH_MINIGAME_VETERAN: ${{ vars.PGS_ACH_MINIGAME_VETERAN }}
          PGS_LB_BUBBLE_POP: ${{ vars.PGS_LB_BUBBLE_POP }}
          PGS_LB_TIMING_BAR: ${{ vars.PGS_LB_TIMING_BAR }}
          PGS_LB_CLEANUP_RUSH: ${{ vars.PGS_LB_CLEANUP_RUSH }}
          PGS_LB_FOOD_DROP: ${{ vars.PGS_LB_FOOD_DROP }}
          PGS_LB_MEMORY_SHELLS: ${{ vars.PGS_LB_MEMORY_SHELLS }}
          PGS_LB_FISH_FOLLOW: ${{ vars.PGS_LB_FISH_FOLLOW }}
        run: |
          for name in PGS_APP_ID PGS_ACH_FIRST_FEED PGS_ACH_SPARKLING_CLEAN PGS_ACH_FIRST_MINIGAME PGS_ACH_HIGH_SCORE PGS_ACH_LEVEL_5 PGS_ACH_LEVEL_10 PGS_ACH_STREAK_3 PGS_ACH_STREAK_7 PGS_ACH_DECORATOR PGS_ACH_COLLECTOR PGS_ACH_MINIGAME_VETERAN PGS_LB_BUBBLE_POP PGS_LB_TIMING_BAR PGS_LB_CLEANUP_RUSH PGS_LB_FOOD_DROP PGS_LB_MEMORY_SHELLS PGS_LB_FISH_FOLLOW; do
            if [ -n "${!name}" ]; then
              echo "$name=${!name}" >> gradle.properties
            fi
          done

      - name: Write local.properties (GitHub Feedback Reporter secrets)
        env:
          FEEDBACK_GITHUB_API_TOKEN: ${{ secrets.FEEDBACK_GITHUB_API_TOKEN }}
          FEEDBACK_GITHUB_REPO_OWNER: ${{ secrets.FEEDBACK_GITHUB_REPO_OWNER }}
          FEEDBACK_GITHUB_REPO_NAME: ${{ secrets.FEEDBACK_GITHUB_REPO_NAME }}
        run: |
          {
            echo "github.api.token=$FEEDBACK_GITHUB_API_TOKEN"
            echo "github.repo.owner=$FEEDBACK_GITHUB_REPO_OWNER"
            echo "github.repo.name=$FEEDBACK_GITHUB_REPO_NAME"
          } >> local.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Build release AAB (App Bundle)
        run: ./gradlew bundleRelease --stacktrace

      - name: Locate build outputs
        id: outputs
        run: |
          APK_PATH=$(find app/build/outputs/apk/release -name "*.apk" | head -n 1)
          AAB_PATH=$(find app/build/outputs/bundle/release -name "*.aab" | head -n 1)
          echo "apk_path=$APK_PATH" >> "$GITHUB_OUTPUT"
          echo "aab_path=$AAB_PATH" >> "$GITHUB_OUTPUT"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-${{ github.sha }}
          path: ${{ steps.outputs.outputs.apk_path }}

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-aab-${{ github.sha }}
          path: ${{ steps.outputs.outputs.aab_path }}

      - name: Clean up sensitive files
        if: always()
        run: |
          rm -f app/release.keystore keystore.properties app/google-services.json
          sed -i '/^github\.api\.token=/d;/^github\.repo\.owner=/d;/^github\.repo\.name=/d' local.properties || true
